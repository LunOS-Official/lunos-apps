# LunOS Official App Repository

GitHub repository: `LunOS-Official/lunos-apps`

Upload `.ln` packages into `packages/`.

GitHub Actions validates the LNPK v2 package and regenerates `repository/index.json`.

LunOS App Store reads that catalog and installs packages through the LunOS package manager.
