# Contributing an LN package

1. Build a valid `.ln` package.
2. Use LNPK v2 when possible.
3. Declare publisher, source visibility, license, permissions and dependencies.
4. Put the package in `packages/`.
5. Commit and push.
6. Wait for GitHub Actions validation.
7. Fix any validation failure before requesting publication.

No private signing key belongs in this repository.
